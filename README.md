# demos-db-mysql

Demos for the MySQL database.

## Layout

- `short/` - single-statement SQL snippets (show server parameters, sleep, turn on the general log).
- `long/` - longer worked demos, each in its own directory (transactions, numeric precision).
- `scripts/` - shell helpers to install, start, stop and query the status of a local MySQL server.
